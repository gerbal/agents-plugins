{
  "mcpServers": {
    "unity": {
      "command": "dotnet",
      "args": [
        "dnx",
        "UnityDevtools.Mcp",
        "--version",
        "0.5.0",
        "--yes"
      ],
      "env": {
        "UNITY_MCP_HOST": "${UNITY_MCP_HOST:-}",
        "UNITY_MCP_PORT": "${UNITY_MCP_PORT:-}",
        "UNITY_MCP_PROCESS": "${UNITY_MCP_PROCESS:-}"
      }
    }
  }
}
